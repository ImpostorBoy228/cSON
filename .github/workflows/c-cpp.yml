name: C/C++ CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: lua
      run: sudo apt install lua5.4 liblua5.4-dev
    - name: make
      run: make
    - name: make tests
      run: make tests
